// code your solution here
function saturdayFun( funActivity = 'roller-skate') {
    return `This Saturday, I want to ${funActivity}!`}
const mondayWork = function (activity = "go to the office") {
    return (`This Monday, I will ${activity}.`)}

    function wrapAdjective(worker = "*") {
        const innerFunction = function (emphatic = "a hard worker") {
        return (`You are ${worker}${emphatic}${worker}!`)}
        return innerFunction 
    }